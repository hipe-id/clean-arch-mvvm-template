package ${packageName}.domain.interactor;

public class ${interactorName}(private final ${repositoryName} ${repositoryName?uncap_first}) {

    // TODO: This is an example of Use case (interactor between repo and model)
}
