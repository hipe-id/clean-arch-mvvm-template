package ${packageName}.domain.interactor;

public class ${interactorName} {

    // TODO: This is an example of Use case (interactor between repo and model)
    private ${repositoryName} ${repositoryName?uncap_first};

    public ${interactorName}(${repositoryName} ${repositoryName?uncap_first}) {
        this.${repositoryName?uncap_first} = ${repositoryName?uncap_first};
    }
}
